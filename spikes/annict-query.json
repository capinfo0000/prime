{
  "query": "query ($season: String!) { searchWorks(seasons: [$season], orderBy: {field: WATCHERS_COUNT, direction: DESC}, first: 50) { nodes { id title media episodesCount seasonName image { recommendedImageUrl facebookOgImageUrl } programs(first: 80) { nodes { startedAt channel { name } episode { number numberText } } } } } }",
  "variables": { "season": "2026-summer" }
}
